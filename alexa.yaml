smart_home:
  endpoint: !secret a_endpoint
  client_id: !secret a_client
  client_secret: !secret a_secret
  filter:
    include_entities:
      - switch.desktop
      - script.night
      - script.morning
      - script.color_loop
      - script.fan_off
      - cover.garage_door
      - cover.bedroom_blinds
      - input_boolean.ac_boost
      - climate.bedroom_ac
      - input_button.activategoveerainbow